# Copy to `.env` or `.env.local` (already gitignored). Required for Expo push token on device.
# Create/link project: run `eas login`, then `eas init` from the mobile folder — paste Project ID below.
EXPO_PUBLIC_EAS_PROJECT_ID=
